import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`lets start`)],
  botName:"Dogesh",
  state:{sname:"prajwal"}
}

export default config